# 🚀 GOLDEN BREEZE - Быстрая шпаргалка

## Запуск сервера (выберите один способ)

### Способ 1: Через PowerShell скрипт (РЕКОМЕНДУЕТСЯ)
```powershell
cd "c:\Users\sveto\OneDrive\Раработка торговых ботов\Golden Breeze"
.\run_server.ps1
```

### Способ 2: Вручную
```powershell
cd "c:\Users\sveto\OneDrive\Раработка торговых ботов\Golden Breeze"
.\venv\Scripts\Activate.ps1
uvicorn aimodule.server.local_ai_gateway:app --host 127.0.0.1 --port 5005 --reload
```

## Тестирование

### Запуск всех тестов:
```powershell
cd "c:\Users\sveto\OneDrive\Раработка торговых ботов\Golden Breeze"
.\run_test.ps1
```

### Или вручную:
```powershell
.\venv\Scripts\Activate.ps1
python test_api.py
```

## API Endpoints

### Health Check
```bash
GET http://127.0.0.1:5005/health
```

### Get Prediction
```bash
POST http://127.0.0.1:5005/predict
Content-Type: application/json

{
  "symbol": "XAUUSD",
  "timeframe": "M5",
  "candles": [...]
}
```

## Быстрое тестирование через curl

```powershell
# Health check
Invoke-WebRequest -Uri "http://127.0.0.1:5005/health" -Method Get

# Predict (пример с файлом)
Invoke-WebRequest -Uri "http://127.0.0.1:5005/predict" -Method Post -ContentType "application/json" -Body (Get-Content test_request.json)
```

## Остановка сервера

Нажмите `CTRL+C` в окне, где запущен сервер

## Структура файлов

- `run_server.ps1` - запуск сервера
- `run_test.ps1` - запуск тестов
- `test_api.py` - полный набор тестов
- `simple_test.py` - быстрая проверка
- `START_HERE.md` - подробная инструкция
- `DEPLOYMENT_REPORT.md` - отчёт о развёртывании
- `README.md` - основная документация

## Полезные команды

```powershell
# Активировать виртуальное окружение
.\venv\Scripts\Activate.ps1

# Деактивировать
deactivate

# Проверить установленные пакеты
pip list

# Обновить requirements.txt
pip freeze > requirements.txt
```

## Типичные проблемы

### Сервер не запускается
1. Проверьте, активировано ли виртуальное окружение
2. Убедитесь, что порт 5005 свободен
3. Проверьте, установлены ли все зависимости: `pip list`

### Тест не проходит
1. Убедитесь, что сервер запущен
2. Проверьте адрес: http://127.0.0.1:5005
3. Попробуйте простой тест: `python simple_test.py`

### Ошибка импорта модулей
1. Активируйте виртуальное окружение
2. Переустановите зависимости: `pip install -r requirements.txt`

---

**Версия:** v2.0.0 (Full AI Integration)  
**Статус:** ✅ Готов к использованию и обучению моделей
