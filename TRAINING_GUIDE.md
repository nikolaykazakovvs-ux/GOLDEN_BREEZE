# 🎓 ИНСТРУКЦИЯ ПО ОБУЧЕНИЮ AI-МОДЕЛЕЙ

## Полный AI-комплект "Golden Breeze" - Training Guide

---

## 📋 Обзор

Проект Golden Breeze теперь включает 3 ML-модели:

1. **LSTM Direction Model** - прогноз направления движения цены
2. **Regime Cluster Model** - определение режима рынка (KMeans/GMM)
3. **Lexicon Sentiment Model** - анализ настроений без внешних API

---

## 🗂️ Подготовка данных

### Шаг 1: Создайте папку для данных

```powershell
cd "c:\Users\sveto\OneDrive\Раработка торговых ботов\Golden Breeze"
mkdir data
```

### Шаг 2: Подготовьте CSV с историческими данными XAUUSD

Формат файла `data/xauusd_m5.csv`:

```csv
timestamp,open,high,low,close,volume
2025-11-30T09:00:00,2640.0,2642.0,2639.0,2641.5,1000.0
2025-11-30T09:05:00,2641.5,2643.0,2640.5,2642.0,1100.0
...
```

**Важно:**
- Минимум 1000 свечей для качественного обучения
- Рекомендуется 10000+ свечей
- Таймфрейм: M5 или выше

### Шаг 3: Используйте готовый скрипт для подготовки данных

```powershell
.\venv\Scripts\Activate.ps1
python aimodule/training/prepare_data_example.py
```

---

## 🤖 1. Обучение LSTM-модели направления

### Что делает модель

Предсказывает краткосрочное направление цены:
- **LONG** - цена пойдёт вверх
- **SHORT** - цена пойдёт вниз
- **FLAT** - цена останется примерно на том же уровне

### Архитектура

- **Вход**: последовательность из 50 свечей (close, delta_close, volume)
- **LSTM слой**: 32 hidden units
- **Выход**: 3 класса (FLAT=0, LONG=1, SHORT=2)

### Запуск обучения

```powershell
cd "c:\Users\sveto\OneDrive\Раработка торговых ботов\Golden Breeze"
.\venv\Scripts\Activate.ps1
python -m aimodule.training.train_direction_lstm
```

### Параметры (можно настроить в скрипте)

- `epochs`: количество эпох (по умолчанию 10)
- `batch_size`: размер батча (по умолчанию 64)
- `lr`: learning rate (по умолчанию 0.001)
- `seq_len`: длина последовательности (по умолчанию 50)

### Результат

Веса модели сохраняются в:
```
aimodule/models/direction_model.pt
```

### Проверка работы

После обучения модель автоматически загружается при запуске сервера.
Если веса отсутствуют — используется fallback (momentum).

---

## 📊 2. Обучение детектора рыночного режима

### Что делает модель

Определяет текущий режим рынка:
- **TREND_UP** - восходящий тренд
- **TREND_DOWN** - нисходящий тренд
- **RANGE** - боковое движение (флэт)
- **VOLATILE** - высокая волатильность

### Метод

Используется **кластеризация** (KMeans или GMM) на основе признаков:
- Разница между быстрой и медленной SMA
- ATR (Average True Range)
- Нормализованный диапазон (high-low)/close

### Запуск обучения (KMeans)

```powershell
cd "c:\Users\sveto\OneDrive\Раработка торговых ботов\Golden Breeze"
.\venv\Scripts\Activate.ps1
python -m aimodule.training.train_regime_cluster
```

### Альтернатива: GMM (Gaussian Mixture Model)

Откройте `aimodule/training/train_regime_cluster.py` и замените:

```python
# Вместо
train_kmeans(df, n_clusters=3)

# Используйте
train_gmm(df, n_components=3)
```

### Параметры

- `n_clusters` / `n_components`: количество кластеров (рекомендуется 3-4)

### Результат

Модель сохраняется в:
```
aimodule/models/regime_model.pt
```

(формат joblib pickle)

### Интерпретация кластеров

После обучения кластеры автоматически маппятся на режимы по эвристике.
Для точной настройки можно вручную отредактировать `_map_cluster_to_regime()` в `regime_model.py`.

---

## 💭 3. Создание лексикона настроений

### Что делает модель

Анализирует текстовый контекст (новости, твиты, заголовки) и возвращает sentiment score от -1.0 до 1.0.

### Метод

Используется **словарь слов** с весами:
- Позитивные слова: "bullish", "rally", "growth" → положительный вес
- Негативные слова: "bearish", "drop", "fall" → отрицательный вес

### Создание лексикона

```powershell
cd "c:\Users\sveto\OneDrive\Раработка торговых ботов\Golden Breeze"
.\venv\Scripts\Activate.ps1
python -m aimodule.training.build_sentiment_lexicon
```

### Результат

Лексикон сохраняется в:
```
aimodule/models/sentiment_model.gguf
```

Формат (текстовый):
```
word,weight
bullish,0.6
bearish,-0.6
rally,0.5
drop,-0.5
...
```

### Расширение лексикона

Отредактируйте `DEFAULT_LEXICON` в `build_sentiment_lexicon.py`, добавьте:

```python
DEFAULT_LEXICON = {
    # Позитив
    "bullish": 0.6,
    "strong": 0.5,
    "breakout": 0.5,
    
    # Негатив
    "bearish": -0.6,
    "weak": -0.5,
    "breakdown": -0.5,
    
    # Ваши слова...
    "support": 0.3,
    "resistance": -0.3,
}
```

### Использование в API

В будущем можно передавать новостной контекст:

```json
{
  "symbol": "XAUUSD",
  "timeframe": "M5",
  "candles": [...],
  "news_context": "Gold shows strong bullish momentum as markets rally"
}
```

---

## 🔄 Workflow полного обучения

### 1. Подготовка данных

```powershell
# Загрузите исторические данные XAUUSD M5
# Сохраните в data/xauusd_m5.csv
```

### 2. Обучение всех моделей

```powershell
cd "c:\Users\sveto\OneDrive\Раработка торговых ботов\Golden Breeze"
.\venv\Scripts\Activate.ps1

# Шаг 1: LSTM Direction
python -m aimodule.training.train_direction_lstm

# Шаг 2: Regime Clustering
python -m aimodule.training.train_regime_cluster

# Шаг 3: Sentiment Lexicon
python -m aimodule.training.build_sentiment_lexicon
```

### 3. Проверка обученных моделей

```powershell
# Проверка наличия весов
ls aimodule\models\

# Должны быть файлы:
# - direction_model.pt
# - regime_model.pt
# - sentiment_model.gguf
```

### 4. Перезапуск сервера

```powershell
# Остановите старый сервер (Ctrl+C)
# Запустите новый
.\run_server.ps1
```

### 5. Тестирование

```powershell
.\run_test.ps1
```

---

## 📈 Мониторинг и улучшение

### Проверка качества LSTM-модели

Добавьте validation в `train_direction_lstm.py`:

```python
from sklearn.model_selection import train_test_split

# После создания dataset
train_idx, val_idx = train_test_split(range(len(dataset)), test_size=0.2)
train_data = Subset(dataset, train_idx)
val_data = Subset(dataset, val_idx)

train_loader = DataLoader(train_data, batch_size=batch_size, shuffle=True)
val_loader = DataLoader(val_data, batch_size=batch_size)

# После каждой эпохи вычисляйте accuracy на валидации
```

### Тюнинг гиперпараметров

**Direction LSTM:**
- Увеличьте `hidden_size` (32 → 64 → 128)
- Добавьте dropout для регуляризации
- Поэкспериментируйте с `seq_len` (30, 50, 100)

**Regime Clustering:**
- Попробуйте разное количество кластеров (2, 3, 4, 5)
- Сравните KMeans и GMM
- Добавьте больше признаков (RSI, MACD)

**Sentiment:**
- Расширьте лексикон финансовыми терминами
- Добавьте веса для фраз (not bullish → negative)
- Интегрируйте готовые модели (FinBERT, FinGPT)

---

## ⚠️ Важные замечания

### Fallback режимы

Все модели работают в fallback-режиме при отсутствии обученных весов:
- **Direction**: price momentum
- **Regime**: SMA-based heuristic
- **Sentiment**: regime-based baseline

### Минимальные требования данных

- **LSTM**: минимум 100 свечей для обучения
- **Clustering**: минимум 500 свечей для стабильных кластеров
- **Sentiment**: работает сразу с дефолтным лексиконом

### Производительность

После обучения моделей:
- Время инференса: < 100ms на запрос
- Память: ~300MB (с загруженными моделями)
- CPU: достаточно для real-time прогнозов

---

## 🎯 Следующие шаги

### Краткосрочные (1-2 недели)

1. Обучите все модели на реальных данных XAUUSD
2. Соберите метрики точности (accuracy, precision, recall)
3. Настройте гиперпараметры

### Среднесрочные (1 месяц)

1. Добавьте больше признаков (RSI, MACD, Bollinger Bands)
2. Реализуйте Transformer вместо LSTM
3. Интегрируйте FinRL для RL-подхода

### Долгосрочные (3+ месяца)

1. Multi-timeframe анализ (M1, M5, M15, H1)
2. Ensemble моделей (voting/stacking)
3. Online learning и адаптация

---

## 📚 Дополнительные ресурсы

### Документация
- `README.md` - основная информация
- `START_HERE.md` - быстрый старт
- `DEPLOYMENT_REPORT.md` - отчёт о развёртывании

### Примеры
- `test_api.py` - тестирование API
- `debug_test.py` - отладка
- `test_imports.py` - проверка импортов

---

**Версия:** v2.0.0 (ML-Ready)  
**Дата:** 30 ноября 2025  
**Статус:** ✅ Готов к обучению и production
