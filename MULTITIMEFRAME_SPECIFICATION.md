# 🌐 Golden Breeze Hybrid Strategy v1.0 - Мультитаймфреймовая Спецификация

## 📋 Обзор

Данный документ описывает **мультитаймфреймовую логику**, встроенную в Golden Breeze Hybrid Strategy v1.0.

### Ключевая концепция

> **"Стратегия всегда читает все таймфреймы для принятия решений"**

Это означает, что:
1. Перед каждым торговым решением стратегия обновляет данные по **всем** таймфреймам (M5, M15, H1, H4)
2. AI Core анализирует **каждый** таймфрейм и возвращает свой прогноз (regime, direction, confidence)
3. **TimeframeSelector** выбирает оптимальный рабочий таймфрейм (`PRIMARY_TF`) на основе текущих условий
4. Торговое решение принимается на `PRIMARY_TF`, но с учётом контекста старших таймфреймов

---

## 🔧 Правка в базовых условиях (раздел 2 ТЗ)

**Было:**
> Базовый таймфрейм анализа: M5

**Стало:**

* **Основной рабочий таймфрейм (PRIMARY_TF):** по умолчанию `M5`, **динамически изменяется**
* **Поддерживаемые таймфреймы (SUPPORTED_TF):** `M1, M5, M15, H1, H4`
* **Интрабарное исполнение:** по тикам или минимум `M1`

`PRIMARY_TF` может **динамически меняться** модулем выбора таймфрейма (TimeframeSelector).

---

## 🧠 Раздел 11: Мультитаймфреймовая логика

### 11.1. Цель

Стратегия должна:

1. **Всегда читать несколько таймфреймов одновременно** (не только один).
2. **Автоматически выбирать "рабочий" таймфрейм (PRIMARY_TF)**, на котором сейчас разумнее всего принимать торговые решения.
3. Использовать **старшие таймфреймы как контекст**, а младшие — для точек входа/выхода и интрабарной логики.

---

### 11.2. Набор таймфреймов

Обязательный минимум:

* **LOW TF (execution):** `M1`
  * для интрабарного исполнения (срабатывание ордеров, SL/TP, трейлинг).

* **MID TF (signal/операционный):** `M5`, `M15`
  * основной кандидат на роль PRIMARY_TF.
  * здесь запрашиваются сигналы от Golden Breeze AI Core.

* **HIGH TF (контекст):** `H1`, `H4`
  * задают "фоновый" тренд, глобальный режим рынка, фильтры по направлению.

```
SUPPORTED_TF = [M1, M5, M15, H1, H4]
```

---

### 11.3. Что AI-ядро видит по таймфреймам

На каждом цикле анализа (например, по закрытию M5):

Стратегия запрашивает у Golden Breeze AI Core:

**Для каждого `tf ∈ {M5, M15, H1, H4}`:**

* `regime(tf)` → `trend_up / trend_down / range / volatile`
* `direction(tf)` → `long / short / flat`
* `direction_confidence(tf)` → `0..1`
* `volatility_score(tf)` (если есть)
* `sentiment(tf)` (можно использовать общий, но с привязкой ко времени)

**M1** используется только для исполнения ордеров, а не для AI-прогнозов.

---

### 11.4. Модуль выбора таймфрейма (TimeframeSelector)

Вводим логический модуль:

> **TimeframeSelector** — принимает решения, какой таймфрейм считать текущим `PRIMARY_TF` для сигналов.

#### Входы селектора:

* `regime(tf)` для M5/M15/H1/H4
* `direction_confidence(tf)`
* `volatility(tf)` или ATR по tf
* (опционально, позже) исторические метрики по tf: ROI, WinRate

#### Выходы селектора:

* `PRIMARY_TF` — текущий рабочий таймфрейм для стратегии (M5 или M15, реже H1)
* `CONTEXT_TF_HIGH` — старший tf для фильтра (H1/H4)
* `EXEC_TF_LOW` — младший tf для исполнения (M1)
* `should_trade` — можно ли вообще торговать сейчас
* `reason` — причина выбора PRIMARY_TF
* `context_filter` — дополнительные фильтры от старших TF

---

### 11.5. Правила выбора PRIMARY_TF (v1.0, простой вариант)

В первой версии задаём **простые, но понятные правила**, без магии:

#### Шаг 1. Фильтр по режиму и уверенности:

* если на **M5**:
  * `direction_confidence(M5) ≥ C_min` (например, 0.65),
  * `regime(M5) ∈ {trend_up, trend_down, range}` (не чистый хаос),

  → **PRIMARY_TF = M5**

* иначе, если на **M15**:
  * `direction_confidence(M15) ≥ C_min`,

  → **PRIMARY_TF = M15**

* если и там плохо — по умолчанию PRIMARY_TF остаётся предыдущим значением
  (стратегия может временно перестать открывать новые сделки).

#### Шаг 2. Фильтр по старшему таймфрейму:

* `regime(H1/H4)` используется как контекст:
  * если на H1/H4 чёткий тренд, а на M5 – шумный range/volatile, это усиливает аргумент перейти на M15 / более крупный взгляд;
  * если старшие TF в хаосе/волатильности, стратегия может уменьшить активность (меньше новых сделок).

#### Шаг 3. Влияние на стратегию:

* Все **логические правила входа** (trend / range / volatile) применяются **на PRIMARY_TF**.
* Старшие таймфреймы:
  * запрещают сделки против "глобального" тренда,
  * помогают выявлять ложные сигналы.

---

### 11.6. Использование всех таймфреймов при каждом решении

Жёсткое требование:

> **"Мультитаймфрейм обязательно читает все таймфреймы для принятия решений."**

**Формализуем:**

Перед принятием любого нового решения о входе/выходе стратегия должна:

1. **Обновить данные** по:
   * M1 (для исполнения / состояния цены),
   * M5, M15 (кандидаты на PRIMARY_TF),
   * H1, H4 (контекст).

2. **Обновить прогнозы AI ядра** по M5/M15/H1/H4 (как минимум режим/направление/уверенность).

3. **Только после этого:**
   * TimeframeSelector выбирает или сохраняет `PRIMARY_TF`.
   * Стратегия применяет гибридные правила для `PRIMARY_TF` с учётом контекста H1/H4.
   * Исполнение ордеров (и SL/TP/трейлинг) — по тикам или M1.

---

### 11.7. Ограничения v1.0

* TimeframeSelector работает по **простым правилам** (confidence + режим + старший тренд),
  без сложной оптимизации и без обучения.
* Метрики по таймфреймам (ROI/WinRate per TF) могут быть добавлены на следующих версиях (v1.1+),
  чтобы селектор сам "учился", на каком TF стратегия работает лучше.

---

## 📐 Архитектура реализации

### Компоненты

1. **`TimeframeSelector`** (`strategy/timeframe_selector.py`)
   - Принимает `TimeframeData` по всем TF
   - Возвращает `TimeframeDecision` с выбранным PRIMARY_TF
   - Методы: `select_timeframe()`, `_apply_selection_rules()`, `_analyze_high_timeframes()`

2. **`AIClient`** (`strategy/ai_client.py`)
   - Метод `predict_multitimeframe()` запрашивает сигналы по всем TF одновременно
   - Fallback: запрос каждого TF отдельно через `predict()`

3. **`HybridStrategy`** (`strategy/hybrid_strategy.py`)
   - `on_new_candle()` принимает `multitf_data: Dict[str, pd.DataFrame]`
   - Шаг 1: `_request_multitf_signals()` → получение AI сигналов
   - Шаг 2: `_select_primary_timeframe()` → выбор PRIMARY_TF
   - Шаг 3: генерация сигнала на PRIMARY_TF
   - Шаг 4: интрабарное исполнение на M1

4. **`BacktestEngine`** (`strategy/backtest_engine.py`)
   - Метод `load_multitf_data(data_dict: Dict[str, pd.DataFrame])`
   - `_sync_multitf_data()` синхронизирует данные по времени
   - Передаёт synced data в `on_new_candle()`

5. **`StrategyConfig`** (`strategy/config.py`)
   - Новые параметры:
     - `primary_tf: str = "M5"` (динамический)
     - `supported_timeframes: List[str] = ["M1", "M5", "M15", "H1", "H4"]`
     - `execution_tf: str = "M1"`
     - `context_tf_high: str = "H1"`
     - `tf_selector_enable: bool = True`
     - `tf_selector_min_confidence: float = 0.65`
     - `tf_selector_high_confidence: float = 0.8`

---

## 📊 Пример работы

```python
from strategy import StrategyConfig, HybridStrategy, BacktestEngine

# Конфигурация
config = StrategyConfig(
    primary_tf="M5",              # Начальный рабочий TF
    tf_selector_enable=True,      # Включить динамический выбор
    tf_selector_min_confidence=0.65,
    tf_selector_high_confidence=0.8
)

# Создание стратегии
strategy = HybridStrategy(config, initial_balance=10000.0)

# Backtesting engine
backtest = BacktestEngine(strategy, config)

# Загрузка мультитаймфреймовых данных
multitf_data = {
    "M5": df_m5,    # DataFrame с OHLCV + индикаторы
    "M15": df_m15,
    "H1": df_h1,
    "H4": df_h4
}
backtest.load_multitf_data(multitf_data)
backtest.load_m1_data(df_m1)  # Для интрабара

# Запуск
backtest.run()
```

---

## 🔄 Workflow (последовательность событий)

### При закрытии M5 свечи:

1. **BacktestEngine** формирует `current_candle` и вызывает:
   ```python
   strategy.on_new_candle(candle, historical_data, multitf_data)
   ```

2. **HybridStrategy.on_new_candle()**:
   - Вызывает `_request_multitf_signals()` → запрос AI по M5/M15/H1/H4
   - Вызывает `_select_primary_timeframe(multitf_signals)` → TimeframeSelector выбирает PRIMARY_TF
   - Проверяет `tf_decision.should_trade`
   - Если `True` → вызывает `_generate_trading_signal()` на PRIMARY_TF
   - Если сигнал валиден → создаёт pending order

3. **Интрабарная обработка** (M1 или тики):
   - BacktestEngine вызывает `strategy.on_tick()` для каждого тика/M1 внутри M5
   - HybridStrategy проверяет:
     - `_check_pending_orders()` → исполнение ордеров
     - `_update_positions()` → SL/TP/trailing

4. **Feedback в AI**:
   - При закрытии позиции → `_send_trade_feedback()` отправляет результат в AI Core

---

## 🎯 Примеры сценариев

### Сценарий 1: Чёткий тренд на M5

```
AI сигналы:
  M5:  regime=trend_up, direction=long, confidence=0.75
  M15: regime=trend_up, direction=long, confidence=0.70
  H1:  regime=trend_up, direction=long, confidence=0.85
  H4:  regime=trend_up, direction=long, confidence=0.80

TimeframeSelector:
  → PRIMARY_TF = M5 (высокая confidence, согласован с H1)
  → should_trade = True
  → context_filter = "H1: тренд long | H4: тренд long"

Результат: Открываем long позицию на M5 (TrendStrategy)
```

### Сценарий 2: M5 в хаосе, M15 чёткий

```
AI сигналы:
  M5:  regime=volatile, confidence=0.45
  M15: regime=range, direction=flat, confidence=0.72
  H1:  regime=range, confidence=0.65
  H4:  regime=trend_down, confidence=0.60

TimeframeSelector:
  → PRIMARY_TF = M15 (M5 недостаточно уверен, переход на M15)
  → should_trade = True
  → context_filter = "H1: range | H4: тренд short"

Результат: Ищем сигналы на M15 (RangeStrategy)
```

### Сценарий 3: Все TF в волатильности

```
AI сигналы:
  M5:  regime=volatile, confidence=0.40
  M15: regime=volatile, confidence=0.50
  H1:  regime=volatile, confidence=0.55
  H4:  regime=volatile, confidence=0.60

TimeframeSelector:
  → PRIMARY_TF = M5 (сохраняем текущий)
  → should_trade = False
  → reason = "Все TF в высокой волатильности, требуется confidence≥0.8"

Результат: NO TRADE (стратегия ждёт)
```

---

## 📈 Преимущества мультитаймфреймовой логики

1. **Адаптивность:** PRIMARY_TF меняется в зависимости от рыночных условий
2. **Фильтрация шума:** Старшие TF помогают избежать ложных сигналов на младших
3. **Точность входа:** Младшие TF (M5) дают точные точки входа при тренде на H1/H4
4. **Защита от хаоса:** При волатильности на всех TF стратегия не торгует
5. **Прозрачность:** Селектор логирует причины выбора PRIMARY_TF

---

## 🔧 Настройка параметров

В `StrategyConfig`:

```python
config = StrategyConfig(
    # Мультитаймфрейм
    primary_tf="M5",                       # Начальный рабочий TF
    supported_timeframes=["M1", "M5", "M15", "H1", "H4"],
    execution_tf="M1",                     # Интрабар
    context_tf_high="H1",                  # Старший контекстный TF
    
    # TimeframeSelector
    tf_selector_enable=True,               # Вкл/выкл динамический выбор
    tf_selector_min_confidence=0.65,       # Мин confidence для работы
    tf_selector_high_confidence=0.8,       # Высокая confidence (фильтр волатильности)
    
    # ... остальные параметры ...
)
```

---

## ⚠️ Ограничения v1.0

1. **Простые правила:** TimeframeSelector использует фиксированные пороги, без ML
2. **Нет self-learning:** Селектор не адаптируется на основе исторических результатов
3. **Фиксированный набор TF:** M1, M5, M15, H1, H4 (нельзя добавить D1 без изменений кода)
4. **AI Core требует реализации:** Endpoint `/predict_multitimeframe` должен быть реализован на сервере

---

## 🚀 Планы на v1.1+

1. **Adaptive TimeframeSelector:**
   - Обучение на основе ROI/WinRate по каждому TF
   - Динамическое изменение порогов confidence

2. **Multi-TF confirmation:**
   - Требование совпадения направлений на 2+ TF перед входом

3. **News filter integration:**
   - Временное отключение торговли за 15 мин до/после новостей

4. **Extended TF support:**
   - Поддержка D1, W1 для долгосрочного контекста

---

## 📝 Заключение

Мультитаймфреймовая логика превращает Golden Breeze Hybrid Strategy из "свечка вверх-вниз" в **настоящую адаптивную систему**, которая:

- **Видит рынок в разных масштабах** одновременно
- **Автоматически выбирает** оптимальный таймфрейм для работы
- **Использует контекст** старших таймфреймов для фильтрации
- **Исполняет внутри свечи** (не по Open/Close)

Это именно та архитектура, которую вы запросили 👍

---

**Версия:** 1.0  
**Дата:** 2024-12-01  
**Статус:** ✅ Реализовано
